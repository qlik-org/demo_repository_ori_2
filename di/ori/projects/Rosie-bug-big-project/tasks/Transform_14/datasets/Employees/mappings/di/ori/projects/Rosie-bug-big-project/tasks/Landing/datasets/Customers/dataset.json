{
  "id": "customers",
  "name": "Customers",
  "type": "LANDING",
  "inputDatasets": [
    {
      "datasetId": "$replicate$dbo_customers",
      "name": "Customers",
      "projectId": "{{ref(project.current.projectId)}}",
      "taskId": "landing-cl0r",
      "schema": "{{task.landing-cl0r:null$_$dbo.schema}}",
      "database": "{{task.landing-cl0r:null$_$dbo.database}}"
    }
  ]
}