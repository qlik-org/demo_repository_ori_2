{
  "id": "products",
  "name": "Products",
  "type": "LANDING",
  "inputDatasets": [
    {
      "datasetId": "$replicate$dbo_products",
      "name": "Products",
      "projectId": "{{ref(project.current.projectId)}}",
      "taskId": "landing_1-sxyp",
      "schema": "{{task.landing_1-sxyp:null$_$dbo.schema}}",
      "database": "{{task.landing_1-sxyp:null$_$dbo.database}}"
    }
  ]
}