{
  "id": "customers",
  "name": "Customers",
  "type": "LANDING",
  "inputDatasets": [
    {
      "datasetId": "$replicate$dbo_customers",
      "name": "Customers",
      "projectId": "{{ref(project.current.projectId)}}",
      "taskId": "lake_landing-2or9",
      "schema": "{{task.lake_landing-2or9:null$_$dbo.schema}}",
      "database": "{{task.lake_landing-2or9:null$_$dbo.database}}"
    }
  ]
}