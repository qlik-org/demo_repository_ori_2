{
  "id": "products",
  "name": "Products",
  "type": "LANDING",
  "inputDatasets": [
    {
      "datasetId": "$replicate$dbo_products",
      "name": "Products",
      "projectId": "{{ref(project.current.projectId)}}",
      "taskId": "onboarding_landing-nywq",
      "schema": "{{task.onboarding_landing-nywq:null$_$dbo.schema}}",
      "database": "{{task.onboarding_landing-nywq:null$_$dbo.database}}"
    }
  ]
}