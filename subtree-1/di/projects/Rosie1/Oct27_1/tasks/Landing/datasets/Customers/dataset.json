{
  "id": "customers",
  "name": "Customers",
  "type": "LANDING",
  "inputDatasets": [
    {
      "datasetId": "$replicate$dbo_customers",
      "name": "Customers",
      "projectId": "{{ref(project.current.projectId)}}",
      "taskId": "landing-mbs2",
      "schema": "{{task.landing-mbs2:null$_$dbo.schema}}",
      "database": "{{task.landing-mbs2:null$_$dbo.database}}"
    }
  ]
}